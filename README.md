## ChessOnline (LAN Multiplayer)
Сетевые шахматы, построенная на C++, Qt6 и PostgreSQL.
Проект поддерживает игру по локальной сети (LAN) для трех участников: один компьютер выступает сервером, остальные подключаются к нему.
### 📋 Требования
- ОС: Windows 10 / 11
- СУБД: PostgreSQL (версия 13+)
- Библиотеки:
	- Qt Framework (6.x)
	- libpqxx (C++ клиент для PostgreSQL)
### 🛠️ Часть 1: Настройка Сервера (Выполняется на одном ПК)
Выберите один компьютер, который будет главным (Сервером). На нем будет храниться база данных и работать серверная часть игры.
### 1. Узнайте IP-адрес Сервера
Этот адрес понадобится для настройки конфигов и кода.
1. Нажмите Win + R, введите cmd, нажмите Enter.
2. Введите команду: ipconfig
3. Запишите IPv4-адрес вашего адаптера
### 2. Настройка базы данных PostgreSQL
1. Установите PostgreSQL.
2. Откройте pgAdmin или psql и создайте пустую базу данных с именем chess (имя используется в коде по умолчанию).
	```CREATE DATABASE chess;```
	Таблицы создавать не нужно — программа создаст их сама при первом запуске.
3. Разрешите удаленные подключения:
	- Перейдите в папку данных (обычно C:\Program Files\PostgreSQL\XX\data).
	- В файле postgresql.conf найдите и измените строку:
		```listen_addresses = '*'```
	- В файле pg_hba.conf добавьте в конец:
		```host    all             all             0.0.0.0/0            scram-sha-256```
	- Перезапустите службу: Win + R -> services.msc -> найдите PostgreSQL -> Перезапустить.
### 3. Настройка Брандмауэра (Firewall)
Чтобы другие компьютеры видели сервер, откройте порты 6666 (игра) и 5432 (база).
Запустите командную строку (cmd) от имени Администратора и выполните:
```
netsh advfirewall firewall add rule name="DurakGame" dir=in action=allow protocol=TCP localport=6666
netsh advfirewall firewall add rule name="chess" dir=in action=allow protocol=TCP localport=5432
```
### ⚙️ Часть 2: Настройка Кода (Перед сборкой)
Эти изменения нужно внести в исходный код перед компиляцией, чтобы клиенты знали, куда подключаться. Используйте IP-адрес, полученный в Части 1.
### 1. Файл DurakOnline.cpp
Укажите IP-адрес игрового сервера:
```// Замените "127.0.0.1" на IP вашего сервера
char *SERVERIP = "192.168.1.10";
```
### 2. Файл database.cpp
Укажите параметры подключения к базе данных:
```// Укажите IP сервера, логин и пароль от PostgreSQL
std::string db_user = "postgres";
std::string db_pass = "ВАШ_ПАРОЛЬ";
std::string db_host = "192.168.1.10"; // Тот же IP сервера
std::string db_port = "5432";
std::string db_name = "chess";
```

### 🚀 Часть 3: Запуск
### Шаг 1. Запуск Сервера (ПК №1)
1. Убедитесь, что PostgreSQL запущен.
2. Запустите DurakOnline.exe.
3. Важно: При первом запуске программа автоматически создаст необходимые таблицы в базе данных chess (благодаря функции InitializeDatabase).
### Шаг 2. Запуск Клиентов (ПК №2 и №3)
1. Сделайте make all для сборки проекта,
2. Запустите созданный .exe файл
3. Нажмите "Регистрация" или "Войти". Приложение подключится к базе данных на Сервере.
4. Начните игру — приложение подключится к игровому сокету на Сервере (порт 6666).